<!DOCTYPE html>
<html>
<head>
    <title>Blockchain Voting</title>
    <style>
        .flash-message {
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
        }
        .success { background-color: #dff0d8; color: #3c763d; }
        .error { background-color: #f2dede; color: #a94442; }
    </style>
</head>
<body>
    <h1>
Blockchain Sistema de Votación - Monitor de capture de voto</h1>
    
    {% with messages = get_flashed_messages(with_categories=true) %}
        {% if messages %}
            {% for category, message in messages %}
                <div class="flash-message {{ category }}">{{ message }}</div>
            {% endfor %}
        {% endif %}
    {% endwith %}
    
    <form action="/vote" method="post">
        <label for="voter_id">Voter ID:</label>
        <input type="text" id="voter_id" name="voter_id" required>
        <br><br>
        <label>Candidate:</label><br>
        {% for candidate in candidates %}
            <input type="radio" id="{{ candidate.id }}" name="candidate_id" value="{{ candidate.id }}" required>
            <label for="{{ candidate.id }}">{{ candidate.name }}</label><br>
        {% endfor %}
        <br>
        <button type="submit">Vote</button>
    </form>
    <p><a href="/results">View Results</a> | <a href="/blockchain">View Blockchain</a></p>
</body>
</html>
